# 키맨 발굴용 회사 연락처 수집 프로그램 — 구현 계획 (plan.md)

> 최종 협의일: 2026-06-01
> 본 문서는 사용자와 협의해 확정한 구현 방안의 기준 문서입니다. 변경 시 이 문서를 먼저 갱신합니다.

---

## 1. 프로젝트 개요

### 1.1 목적
키맨(직장 내 직책자)에게 접근하기 위한 **회사 단위 연락처/정보를 정확하게 수집**하는 데스크톱 프로그램.
영업·상품 설명을 위해, 회사 대표번호로 직접 연락해 키맨에게 도달하는 것을 목표로 한다.

### 1.2 키맨 정의
특정 모임·직장·지역사회에서 영향력이 커서 사람들을 모아 상품 설명을 가능하게 해주는 사람.
본 프로젝트에서는 **직장 내 직책자(팀장, 본부장 등)** 를 의미한다.
→ 따라서 개인을 직접 특정하기보다 **회사 + 조직(본부/팀) 구조 + 회사 대표번호**를 수집하고, 키맨은 **직책으로 식별**한다.

### 1.3 핵심 원칙 (가장 중요)
- **유추 금지·정확성 최우선**: 수집된 모든 값은 실제 출처에 존재하는 정보여야 한다. **없는 정보는 빈칸**으로 두며 절대 추측/생성하지 않는다.
- **출처(provenance) 기록**: 모든 필드는 "어디서(소스/URL), 언제(수집시각)" 가져왔는지 함께 저장한다.
- **검증 후 저장**: 전화번호 등은 형식 검증을 거쳐 저장하고, 불확실한 값은 `unverified` 플래그로 표시한다.

---

## 2. 협의 결과 요약 (확정 사항)

| 항목 | 결정 |
|---|---|
| **데이터 소스** | 민간 구인구직 사이트(사람인 등)를 **기본**으로 수집 → 부족 필드를 **공공데이터 + 회사 홈페이지**로 보강 (민간 우선 하이브리드) |
| **기술 스택** | **Electron + Node.js 단일 스택** (스크래핑은 Playwright) |
| **AI API** | **후순위/선택적** — 1차는 규칙기반(정규식·HTML 파싱), AI는 이후 옵션으로 추가 |
| **연락처 목표** | **회사 대표번호 = 필수 수집** / 키맨 개인번호 = 공개 시에만 수집(best-effort) |

### 2.1 기본값으로 확정한 세부 (다르면 알려주세요)
- 렌더러 UI: **React + TypeScript** (electron-vite 스캐폴드) — 테이블/필터 UI에 유리
- 저장소: **SQLite** (`better-sqlite3`) — 중복 제거·조회·증분 수집에 유리
- 수집 사이트: **UI 필터에서 복수 선택**(사람인·잡코리아 등 체크박스). 어댑터 구현 순서는 사람인 → 잡코리아 → 인크루트 등으로 진행하되, **UI에는 선택지로 함께 노출**한다.

---

## 3. 수집 대상 정보 (필드 정의)

| 필드 | 한글 | 필수 | 비고 |
|---|---|:---:|---|
| `company_name` | 회사명 | ✅ | 정규화(법인격 표기 통일) |
| `main_phone` | **회사 대표번호** | ✅ | 지역번호 유선 / 070 / 15xx·16xx·18xx 대표번호. 형식 검증 필수 |
| `address` | 주소 | ✅ | 시/도·시군구 파싱 가능하면 분리 저장 |
| `industry` | 업종 | ✅ | 소스 표기 그대로 + (가능 시) 표준 분류 |
| `employee_count` | 근로자 수 | ✅ | 공공데이터(국민연금 가입자수) 보강 활용 |
| `departments` | 조직 내 본부/팀 이름(복수) | ⬜ | 상세 공고/조직 페이지에 있을 때만 |
| `keyman_candidates` | 키맨 후보(이름·직책·연락처) | ⬜ | **공개된 경우만** best-effort |
| `homepage_url` | 회사 홈페이지 링크 | ⬜ | 보강 수집의 진입점으로도 사용 |
| `biz_reg_no` | 사업자등록번호 | ⬜ | 있으면 중복 제거 키로 최우선 사용 |
| `source` / `source_url` | 출처 / 출처 URL | ✅ | 필드별 출처 추적 |
| `collected_at` | 수집 시각 | ✅ | |

> 키맨 개인번호는 "있으면 알려주고 없으면 패스". 실제 도달은 **회사 대표번호로 직접 전화**해 추후 확인하는 운영 방식.

### 3.1 전화번호 검증 규칙 (정확성 핵심)
수집/정규화 시 아래 패턴으로 분류·검증한다.

- **회사 대표번호(필수 대상)**
  - 지역번호 유선: `02`, `031`, `032`, `033`, `041`, `042`, `043`, `044`, `051`, `052`, `053`, `054`, `055`, `061`, `062`, `063`, `064`
  - 인터넷전화: `070`
  - 전국대표번호: `15xx`, `16xx`, `18xx` (예: 1588, 1577, 1644)
- **휴대폰**: `010`(및 구 `011/016/017/018/019`) → 키맨 개인번호 후보로 별도 분류(best-effort)
- 정규화: 숫자만 추출 → 하이픈 포맷 통일 → 유효 패턴 불일치 시 `unverified` 플래그, 저장은 하되 UI에서 구분 표시

---

## 4. 데이터 소스 전략 (민간 우선 하이브리드)

수집 파이프라인 순서:

```
[1] 민간 사이트 검색·수집 (기본)        → 회사명·대표번호·주소·업종·근로자수·(조직)
        │   (사람인 → 이후 잡코리아 등)
        ▼
[2] 부족 필드 보강
        ├─ 공공데이터 (합법·정형)
        │     • 워크넷/고용24 채용 OpenAPI (공공데이터포털)
        │     • 국민연금 가입사업장 공개데이터 (주소·업종·가입자수≈근로자수)
        └─ 회사 홈페이지 크롤링 (대표번호·추가 정보·조직 페이지)
        ▼
[3] 병합·정규화 → 중복 제거(dedup) → 저장
```

- **민간 사이트**: 검색 필터로 후보 기업 리스트 → 상세 페이지 파싱.
- **공공데이터/홈페이지**: 민간에서 비어 있는 필수 필드(특히 대표번호·주소·근로자수)를 채우는 보강용.
- 어댑터 패턴으로 소스를 모듈화해 추가/교체가 쉽도록 한다(§5.3).

---

## 5. 아키텍처

### 5.1 기술 스택
- **Electron** (데스크톱 GUI, Windows 우선)
- **Main 프로세스 (Node.js/TypeScript)**: 수집 오케스트레이션, 스크래핑(**Playwright**), 저장(**better-sqlite3**), 로깅, 내보내기
- **Renderer (React + TypeScript, Vite)**: 검색/데이터/내보내기/설정 UI
- **IPC**: preload의 contextBridge로 안전한 `window.api` 노출
- 빌드/배포: `electron-vite` + `electron-builder`

> **외부 API 키 구분(서로 무관·별개)**
> - **공공데이터포털 API**(data.go.kr): 데이터 가져오기용. **무료**, 회원가입 후 인증키 발급. Phase 2에서 사용.
> - **ChatGPT(OpenAI) API**: AI 텍스트 처리용. **유료**, 결제수단 필요. Phase 4(옵션)에서만 사용.

### 5.2 프로세스 분리
- 무거운 수집 작업은 Main 프로세스(필요 시 별도 Worker/UtilityProcess)에서 실행해 UI 블로킹 방지.
- 진행률·로그는 IPC 이벤트로 Renderer에 실시간 스트리밍.

### 5.3 소스 어댑터 인터페이스
```ts
interface SourceAdapter {
  id: string;                  // 'saramin' | 'jobkorea' | 'worknet' | 'nps' | 'homepage'
  search(filters: Filters, opts: RunOptions): AsyncIterable<CompanyRef>;
  fetchDetail(ref: CompanyRef): Promise<RawRecord>;   // 출처 메타 포함
}
```
- 신규 사이트 추가 = 어댑터 1개 구현. 파이프라인/UI 변경 없음.

### 5.4 폴더 구조(예시)
```
key/
  package.json
  electron.vite.config.ts
  tsconfig.json
  src/
    main/
      index.ts
      ipc/                 # IPC 핸들러 (수집 시작/중지, 조회, 내보내기)
      core/
        sources/           # 소스 어댑터
          types.ts
          saramin.ts       # Phase 1
          jobkorea.ts      # Phase 3
          worknet.ts       # 공공데이터 API (Phase 2)
          nps.ts           # 국민연금 가입사업장 (Phase 2)
          homepage.ts      # 홈페이지 보강 (Phase 2)
        pipeline.ts        # 수집→병합→보강→dedup→저장
        normalize.ts       # 전화/주소/회사명 정규화·검증
        dedup.ts
        rateLimiter.ts     # 안전 수집(지연·동시성·backoff)
        ai/                # (옵션, Phase 4) 구조화 추출
      db/
        schema.sql
        repository.ts      # better-sqlite3
      log/
        logger.ts          # txt 로그
      export/
        csv.ts
        xlsx.ts            # exceljs
    preload/index.ts
    renderer/
      index.html
      src/
        App.tsx
        tabs/{SearchTab,DataTab,ExportTab,SettingsTab}.tsx
        components/
        api.ts             # window.api 브리지
  logs/                    # 런타임 로그(txt) — 디버깅용
  data/                    # SQLite DB 파일
```

---

## 6. 데이터 모델

### 6.1 2계층 저장 (출처 추적 + 병합 결과)
- `raw_records`: 소스별 원본 1건씩 저장(source, source_url, raw_json, collected_at). **유추 금지 검증의 근거**.
- `companies`: 병합·정규화된 최종 기업 레코드. 각 필드에 **출처 태그**(어느 raw_record에서 왔는지) 보관.

### 6.2 companies 주요 컬럼
`id, company_name, main_phone, phone_status('verified'|'unverified'), address, region, industry, employee_count, homepage_url, biz_reg_no, departments(json), keyman_candidates(json), field_sources(json), created_at, updated_at`

### 6.3 중복 제거(dedup) 키 우선순위
1. `biz_reg_no` (있으면 최우선)
2. 정규화된 `company_name` + 정규화된 `address`
3. 정규화된 `company_name` + `main_phone`

→ 중복 발견 시 병합(더 신뢰도 높은 출처 우선), 충돌 값은 둘 다 보관하고 UI에서 표시.

---

## 7. UI 구성 (탭)

### 7.1 검색/수집 탭
- **수집 사이트(소스) 선택**: 사람인 / 잡코리아 / (이후 인크루트·워크넷 등) — **복수 선택 체크박스**. 선택한 사이트들에서 수집한다.
- 필터 입력: **지역**(시/도·시군구), **직종/직무**, **검색어(키워드)** — 사이트별 필터 체계에 매핑
- 수집 옵션: 최대 수집 건수, 요청 간 지연(rate limit), AI 보강 on/off(Phase 4)
- 컨트롤: [수집 시작] / [중지] / [재개]
- 표시: 진행률, 실시간 수집 로그(스트리밍), 차단 감지 알림

### 7.2 데이터 탭
- 수집 결과 테이블: 정렬·검색·컬럼 필터
- 행 클릭 → 상세 패널(필드별 **출처/URL** 표시, `unverified` 구분)
- 중복 표시, 수동 편집/삭제(편집 시에도 출처는 'manual'로 기록)

### 7.3 내보내기 탭
- **CSV / XLSX** 내보내기 (현재 필터·선택 컬럼 반영, `exceljs` 사용)
- 저장 경로 선택, 내보내기 결과 요약

### 7.4 설정 탭
- AI API 키(Phase 4), rate limit 기본값, 소스 토글, 로그 레벨, 데이터/로그 경로

---

## 8. 안전 수집 · 로깅 정책

### 8.1 안전 수집 (민간 사이트 차단·부하 방지)
- 요청 간 **지연**(기본 2~5초, 설정 가능) + 약간의 랜덤 지터
- **동시성 제한**(기본 1~2)
- 오류/차단 시 **지수 backoff**, 일정 횟수 초과 시 자동 중단
- **차단 감지**(캡차/비정상 응답) 시 즉시 정지 + 로그 + 사용자 알림
- 명시적 User-Agent, robots.txt 확인, **중단 후 재개(resume)** 가능하도록 진행 상태 저장
- 목적: 과도한 트래픽을 유발하지 않는 "예의 있는 수집". 공격적 우회는 하지 않음.

### 8.2 로깅 (디버깅용 txt)
- 위치: `logs/`
  - `app-YYYYMMDD.txt` : 앱 전반 로그
  - `collect-<runId>.txt` : 수집 실행 단위 로그
- 레벨: `DEBUG / INFO / WARN / ERROR`
- 기록 내용: 요청 URL·상태코드·소요시간, 파싱 결과 요약, 예외(스택 포함), 차단 감지, dedup/병합 결정
- 날짜별 로테이션. 디버깅 시 문제 재현·원인 추적이 가능하도록 충분히 상세하게 남김.

---

## 9. 법적 · 약관 고려사항 (운영 주의)

- 민간 구인구직 사이트는 **이용약관에서 크롤링을 제한**할 수 있고 차단 기술이 있다. 안정성·합법성을 위해 가능한 필드는 공공데이터/홈페이지 보강을 우선 활용한다.
- 수집 데이터는 **주로 기업 단위 정보**(대표번호·주소·업종 등)로 개인정보 비중이 낮다.
- **키맨 개인 연락처**는 공개된 경우에 한해 best-effort로만 수집하며, 개인정보보호법(최소 수집·목적 제한) 취지를 고려한다.
- 실제 사이트별 약관·robots 준수 및 수집·활용에 대한 최종 책임은 운영자(사용자)에게 있다. 본 도구는 안전장치(rate limit·차단 감지·중단)를 기본 제공한다.

---

## 10. 개발 단계 (Phase)

| 단계 | 내용 | 산출물 |
|---|---|---|
| **Phase 0** | 스캐폴딩(electron-vite + React + TS), 로깅, SQLite, 3+1탭 셸, IPC 골격 | 실행되는 빈 앱 + 로그/DB 동작 |
| **Phase 1** | **다중 사이트 선택 UI** + **사람인** 어댑터(필터·검색·상세 파싱) → 회사명·**대표번호**·주소·업종·근로자수, 전화 검증, dedup, 데이터 탭 표시, CSV/XLSX 내보내기 | 핵심 수집·조회·내보내기 + 사이트 선택 골격 |
| **Phase 2** | **잡코리아 어댑터 추가**(선택지에 실동작 연결), 공공데이터(워크넷 OpenAPI·국민연금 가입사업장)+홈페이지 보강으로 부족 필드 채우기, 조직(본부/팀) 규칙기반 추출 | 사이트 2개 + 데이터 완성도 |
| **Phase 3** | 인크루트 등 추가 어댑터, 안전 수집 고도화(backoff·재개), 대량 수집 안정화 | 소스 확장·견고성 |
| **Phase 4 (옵션)** | AI API 통합 — 비정형 텍스트에서 조직/직책 **구조화 추출**(유추 금지·출처 유지) | 조직/직책 추출 품질 향상 |

---

## 11. 추후 협의/미정 사항
- 사람인 외 우선 추가할 민간 사이트 순서(잡코리아/인크루트/워크넷 등)
- 직종/지역 필터의 정확한 분류 체계(사람인 코드 매핑 범위)
- 공공데이터 API 키 발급 주체(공공데이터포털 계정)
- AI 사용 시 모델·예산 한도(Phase 4 진입 시점에 결정)
- 배포 형태(설치 파일 vs 폴더 실행), 자동 업데이트 필요 여부
